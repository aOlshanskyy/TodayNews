<div class="col-md-4">
<div class="brd" style=" outline: 0.05px groove #010;">

	<h3><%=link_to article.title, article_path(article.id) %></h3>
<small>
			<time><%= article.created_at.to_date.strftime("%d %B, %Y") %></time>
</small>
<p><%= sanitize article.shortbody %></p>
</div>
</div>